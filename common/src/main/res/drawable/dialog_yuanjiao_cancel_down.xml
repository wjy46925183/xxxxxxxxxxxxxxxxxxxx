<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充颜色 -->
    <solid android:color="@color/bg_colors_dialog_title" />
    <!-- 线的宽度，颜色绿色 -->
    <stroke
        android:width="1dp"
        android:color="@color/bg_colors_dialog_green" />
    <!-- 矩形的圆角半径 -->
    <corners
        android:bottomLeftRadius="2dp"/>
</shape>
